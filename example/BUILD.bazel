load("//tools/example_test:defs.bzl", "example_test")

example_test(
    name = "oci",
    srcs = glob(["oci/**/*"]),
    workspace_template = "local.WORKSPACE",
)

example_test(
    name = "docker",
    srcs = glob(["docker/**/*"]),
    workspace_template = "local.WORKSPACE",
)
