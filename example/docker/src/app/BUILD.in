load("@rules_python//python:defs.bzl", "py_binary")
load("@pycross_image//rules:oci.bzl", "py_image")
#load("@container_structure_test//:defs.bzl", "container_structure_test")

py_binary(
    name = "app",
    srcs = ["app.py"],
    deps = [
        "@pypi//:grpclib",
        "@pypi//:numpy",
    ],
)

py_image(
    name = "image",
    binary = ":app",
)

# container_structure_test(
#     name = "test",
#     configs = ["test.yaml"],
#     image = ":image",
# )
